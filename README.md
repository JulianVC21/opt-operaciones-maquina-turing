# MAQUINA DE TURING PARA OPTIMIZAR FUNCIONES DISCRETAS Y CONTINUAS

## Pasos:

instalar el entorno virtual:

```bash
python3 -m venv .venv
```

iniciar el entorno virtual:

```bash
.venv\Scripts\python
```

instalar paquetes:
```bash
pip install -r requirements.txt
```
